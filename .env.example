# API Security
# Required: API key for endpoint authentication
API_KEY=

# OpenAI Configuration
# Enable OpenAI adapter by setting your API key
OPENAI_API_KEY=# Optional: Override the default model (defaults to gpt-3.5-turbo)
OPENAI_MODEL=gpt-4.1

# Model Configuration
# Optional: Override the default transformer model for semantic similarity
TRANSFORMER_MODEL=paraphrase-MiniLM-L6-v2
# Optional: Override the default spaCy model for NER
SPACY_MODEL=en_core_web_sm

# Pipeline Configuration
# Similarity threshold for "pass" results (0.0 to 1.0)
SIMILARITY_THRESHOLD=0.7
# Cache TTL in seconds for URL content and LLM results
CACHE_TTL=300
# HTTP timeout in seconds
HTTP_TIMEOUT=10